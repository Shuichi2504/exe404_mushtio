name: Build and deploy ASP.Net Core app to Azure Web App - MUSHTIO

on:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: MUSHTIO deployment disabled
        run: echo "MUSHTIO is disabled. This repository deploys the API to MUSHTIO1 only."
